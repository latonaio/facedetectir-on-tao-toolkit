version: "3"
services:
  facedetectir-tao-toolkit:
    container_name: facedetectir-tao-toolkit
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app/src
    working_dir: /app/src
    tty: true
